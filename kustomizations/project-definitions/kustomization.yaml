apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Only Project resources - these create namespaces via the Workspace controller
  - ../../resources/workspaces/dm-dev-workspace/projects/dm-dev-project/dm-dev-project.yaml

