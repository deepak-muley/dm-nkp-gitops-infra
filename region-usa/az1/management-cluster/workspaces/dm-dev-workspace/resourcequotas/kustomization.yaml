apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Re-enabled: LimitRange policy now provides default resource limits for pods
  # This ResourceQuota increases secrets limit from 50 (default) to 200
  - dm-dev-workspace-quota.yaml

