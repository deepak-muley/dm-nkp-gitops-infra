apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Only Project definitions - these create namespaces via the Workspace controller
  - ./dm-dev-project/dm-dev-project.yaml
