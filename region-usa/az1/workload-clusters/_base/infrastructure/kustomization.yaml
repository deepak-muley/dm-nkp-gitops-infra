apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base infrastructure components shared across workload clusters
# Clusters can reference these as bases and apply overlays
resources:
  - sealed-secrets-controller/
  - gatekeeper/
  # - kyverno/  # Disabled - causing webhook issues blocking other deployments
