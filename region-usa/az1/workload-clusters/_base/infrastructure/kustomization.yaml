apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base infrastructure components shared across workload clusters
# Clusters can reference these as bases and apply overlays
resources:
  - sealed-secrets-controller/
  # Gatekeeper policies (if Gatekeeper is installed)
  # - gatekeeper/
  # Kyverno policies (if Kyverno is installed)
  # - kyverno/
# Example resources to add:
#   - cert-manager/
#   - ingress-nginx/
#   - external-secrets/

